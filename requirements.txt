# Core TTS
TTS==0.22.0                 # Coqui TTS (XTTS v2)

# Torch must match your CUDA; setup.sh installs the CUDA 12.1 wheel.
# Pinned here for reproducibility if someone uses `pip install -r` directly.
torch==2.1.2
torchaudio==2.1.2

# Audio I/O and processing
soundfile>=0.12.1
numpy>=1.24,<2.0
scipy>=1.11
librosa>=0.10.1
pydub>=0.25.1
pyloudnorm>=0.1.1

# Text processing
nltk>=3.8.1
regex>=2023.10.3

# CLI / API
typer>=0.9.0
rich>=13.7.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Misc
tqdm>=4.66
pyyaml>=6.0.1
